// Expressions (produces a value)
3 + 4
1994
true && false && !false


// Statements does not produce a value (if-else/ switch statements)
if (28 > 18) {
    const str = '28 is higher';
}

console.log(`I'm ${2024 - 1994}`); // is an expression as it can be included;

// console.log(`I'm ${2024 - 1994} and ${if (20 > 18) { }}`); // is an statement as it can't be included.
